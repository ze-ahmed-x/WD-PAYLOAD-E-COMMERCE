import React from 'react'

import { Header } from '../../../../payload/payload-types'

const HeaderMobileNav = ({ header }: { header: Header }) => {
  return (
    <div>HeaderMobileNav</div>
  )
}

export default HeaderMobileNav
